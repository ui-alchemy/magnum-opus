= content_for(:stylesheets_block) do
  = stylesheet_link_tag "login"

= content_for(:javascripts_block) do
  = javascript_include_tag "converge-ui/vendor/jquery/jquery-1.6.2.js"
  = javascript_include_tag "converge-ui/login.js"

= content_for(:project_name) do
  = image_tag("logo.png", :alt => get_string(:example_logo))

= content_for(:forgot_password_link) do
  = link_to get_string(:forgot_username_password), password_resets_path

= content_for(:show_password) do
  = get_string(:show_password)

= content_for(:footer) do
  = render :partial => "common/footer"
